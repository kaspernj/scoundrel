rvm: true
builds:
  build_1:
    name: Ruby - PHP
    script:
      - cd ruby/php && bundle && bundle exec rspec
  build_2:
    name: Ruby - Ruby
    script:
      - cd ruby/ruby && bundle && bundle exec rspec
